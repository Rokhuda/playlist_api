version: '3'
services:
  app:
    container_name: playlistdb
    restart: always
    build: ./
    ports:
      - '80:3090'
    volumes:
      - .:/app
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'